<!DOCTYPE html>
<html>
<head>
  <title>Count Vowels in a String</title>
</head>
<body>
  <h2>Task 4: Count Vowels in a String</h2>
  <p>Enter any word or sentence to count how many vowels it has:</p>

  <input type="text" id="textInput" placeholder="Enter a string">
  <button onclick="countVowels()">Count Vowels</button>

  <p id="result"></p>

  <script>
    function countVowels() {
      let str = document.getElementById("textInput").value.toLowerCase();

      if (str === "") {
        document.getElementById("result").innerText = "Please enter a string.";
        return;
      }

      let vowels = "aeiou";
      let count = 0;

      for (let char of str) {
        if (vowels.includes(char)) {
          count++;
        }
      }

      document.getElementById("result").innerText = "Number of vowels: " + count;
    }
  </script>
</body>
</html>
