<!DOCTYPE html>
<html>
<head>
  <title>Count Digits of a Number</title>
</head>
<body>
  <h2>Task 8: Count Digits of a Number</h2>
  <p>Enter a number to count how many digits it has:</p>

  <input type="number" id="numInput" placeholder="Enter number">
  <button onclick="countDigits()">Count Digits</button>

  <p id="result"></p>

  <script>
    function countDigits() {
      let num = document.getElementById("numInput").value;

      if (num === "") {
        document.getElementById("result").innerText = "Please enter a number.";
        return;
      }

      // Convert number to string and get its length
      let count = num.toString().length;

      document.getElementById("result").innerText = 
        "Number of digits: " + count;
    }
  </script>
</body>
</html>
